{% extends 'admin/base.html.twig' %}

{% block title %}Edit Level{% endblock %}

{% block left_sidebar %}
		{{ render(controller(
        'App\\Controller\\DefaultController::left',
        { 'active' : 'level' }
    )) }}

{% endblock %} 

{% block page %}
     <!-- Panel Full Example -->
    {{ form_start(form,{'attr': {'id' : 'exampleFullForm', 'autocomplete':'off' }}) }}
            {% include 'level/_form.html.twig' with {form: form, 'button_label': 'Modifier'} %}
    {{ form_end(form) }}
    <!-- End Panel Full Example -->
{% endblock %}

